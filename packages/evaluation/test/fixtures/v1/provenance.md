# Phase 3 evaluation fixture provenance

`passing-experiment.json` is a deterministic synthetic fixture. It exercises
paired context replay, exact next-action agreement, critical-field and
provenance preservation, recorded usage, latency, and final outcome scoring.

The fixture does not represent a live model result and must not be cited as an
empirical token-saving result.
