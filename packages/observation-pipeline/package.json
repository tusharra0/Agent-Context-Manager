{
  "name": "@acm/observation-pipeline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretest": "pnpm --filter @acm/core build && pnpm --filter @acm/harness-port build && pnpm --filter @acm/reducers build && pnpm --filter @acm/event-store build",
    "test": "vitest run --pool=forks --maxWorkers=1",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.benchmark.json",
    "benchmark": "tsx benchmark/local-paired-run.ts"
  },
  "dependencies": {
    "@acm/core": "workspace:*",
    "@acm/event-store": "workspace:*",
    "@acm/harness-port": "workspace:*",
    "@acm/reducers": "workspace:*",
    "zod": "^4.1.5"
  }
}
